# quick_api_test.py\n\"\"\"\n🚀 Швидкий тест H3 Modal API з реальною базою даних\nПростий скрипт для перевірки основних endpoints\n\"\"\"\n\nimport requests\nimport json\nfrom datetime import datetime\nimport time\n\n# Конфігурація\nBASE_URL = \"http://localhost:8000\"\nTEST_H3_INDICES = {\n    7: \"871fb4662ffffff\",   # Район\n    8: \"881fb46622fffff\",   # Частина району  \n    9: \"891fb466227ffff\",   # Квартал\n    10: \"8a1fb46622d7fff\"   # Вулиця\n}\n\ndef colored_print(text, color=\"white\"):\n    \"\"\"Кольоровий вивід для Windows\"\"\"\n    colors = {\n        \"red\": \"\\033[91m\",\n        \"green\": \"\\033[92m\", \n        \"yellow\": \"\\033[93m\",\n        \"blue\": \"\\033[94m\",\n        \"purple\": \"\\033[95m\",\n        \"cyan\": \"\\033[96m\",\n        \"white\": \"\\033[97m\",\n        \"reset\": \"\\033[0m\"\n    }\n    print(f\"{colors.get(color, '')}{text}{colors['reset']}\")\n\ndef test_endpoint(method, url, params=None, expected_status=200, description=\"\"):\n    \"\"\"Тестування одного endpoint\"\"\"\n    print(f\"\\n{'='*60}\")\n    colored_print(f\"🧪 ТЕСТ: {description}\", \"cyan\")\n    colored_print(f\"📡 {method.upper()} {url}\", \"blue\")\n    if params:\n        colored_print(f\"📋 Параметри: {params}\", \"yellow\")\n    print(f\"{'='*60}\")\n    \n    try:\n        start_time = time.time()\n        \n        if method.lower() == 'get':\n            response = requests.get(url, params=params, timeout=10)\n        else:\n            response = requests.request(method, url, params=params, timeout=10)\n        \n        duration = (time.time() - start_time) * 1000\n        \n        colored_print(f\"⏱️ Час відповіді: {duration:.0f}ms\", \"white\")\n        colored_print(f\"📊 HTTP статус: {response.status_code}\", \"white\")\n        \n        if response.status_code == expected_status:\n            colored_print(f\"✅ SUCCESS: Статус {response.status_code}\", \"green\")\n            \n            try:\n                data = response.json()\n                colored_print(f\"📦 JSON розмір: {len(json.dumps(data))} символів\", \"white\")\n                \n                # Показуємо ключові поля\n                if isinstance(data, dict):\n                    colored_print(f\"🔑 Основні ключі: {list(data.keys())[:5]}\", \"white\")\n                    \n                    # Спеціальний вивід для різних типів відповідей\n                    if 'status' in data:\n                        colored_print(f\"📈 Статус: {data['status']}\", \"green\" if data['status'] == 'success' else \"yellow\")\n                    \n                    if 'location_info' in data:\n                        loc = data['location_info']\n                        colored_print(f\"📍 H3-{loc.get('resolution', 'N/A')}: {loc.get('h3_index', 'N/A')[:12]}...\", \"cyan\")\n                        colored_print(f\"📐 Координати: {loc.get('center_lat', 'N/A'):.6f}, {loc.get('center_lon', 'N/A'):.6f}\", \"cyan\")\n                    \n                    if 'neighbor_coverage' in data:\n                        cov = data['neighbor_coverage']\n                        colored_print(f\"🔗 Покриття: {cov.get('rings', 'N/A')} кілець, {cov.get('hexagon_count', 'N/A')} гексагонів\", \"purple\")\n                    \n                    if 'poi_details' in data:\n                        poi_count = len(data['poi_details'])\n                        colored_print(f\"🏪 POI знайдено: {poi_count}\", \"green\")\n                    \n                    if 'competitors_found' in data.get('competitive_analysis', {}):\n                        comp_count = data['competitive_analysis']['competitors_found']\n                        colored_print(f\"🏢 Конкурентів: {comp_count}\", \"yellow\")\n                        \n                return True, data\n                \n            except json.JSONDecodeError:\n                colored_print(f\"⚠️ Відповідь не є валідним JSON\", \"yellow\")\n                colored_print(f\"📄 Текст: {response.text[:100]}...\", \"white\")\n                return response.status_code == expected_status, response.text\n                \n        else:\n            colored_print(f\"❌ FAILED: Статус {response.status_code} (очікувався {expected_status})\", \"red\")\n            colored_print(f\"📄 Помилка: {response.text[:200]}...\", \"red\")\n            return False, response.text\n            \n    except requests.exceptions.ConnectionError:\n        colored_print(f\"❌ CONNECTION ERROR: Сервер недоступний\", \"red\")\n        colored_print(f\"💡 Запустіть сервер: python start_test_server.py\", \"yellow\")\n        return False, \"Connection failed\"\n        \n    except requests.exceptions.Timeout:\n        colored_print(f\"❌ TIMEOUT: Запит перевищив 10 секунд\", \"red\")\n        return False, \"Timeout\"\n        \n    except Exception as e:\n        colored_print(f\"❌ UNEXPECTED ERROR: {str(e)}\", \"red\")\n        return False, str(e)\n\ndef main():\n    \"\"\"Основна функція тестування\"\"\"\n    colored_print(f\"🚀 H3 MODAL API - ШВИДКИЙ ТЕСТ З БАЗОЮ ДАНИХ\", \"cyan\")\n    colored_print(f\"🌐 Base URL: {BASE_URL}\", \"blue\")\n    colored_print(f\"📅 Час: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\", \"white\")\n    \n    tests = []\n    \n    # 1. Health Check\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/health\",\n        description=\"Health Check - основний стан сервера\"\n    )\n    tests.append((\"Health Check\", success))\n    \n    # 2. Database Connection Test\n    success, data = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/database/test-connection\",\n        description=\"Database Connection - тест підключення до PostgreSQL\"\n    )\n    tests.append((\"Database Connection\", success))\n    \n    # Якщо БД недоступна, показуємо попередження але продовжуємо\n    if not success or (isinstance(data, dict) and data.get('status') == 'error'):\n        colored_print(\"\\n⚠️ УВАГА: База даних недоступна!\", \"yellow\")\n        colored_print(\"Endpoints працюватимуть з mock даними\", \"yellow\")\n    \n    # 3. Coverage Calculator (не потребує БД)\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/coverage-calculator\",\n        params={\"resolution\": 10, \"rings\": 2},\n        description=\"Coverage Calculator - H3 математичні розрахунки\"\n    )\n    tests.append((\"Coverage Calculator\", success))\n    \n    # 4. Analysis Preview\n    h3_index = TEST_H3_INDICES[10]\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/analysis-preview/{h3_index}\",\n        params={\"resolution\": 10},\n        description=\"Analysis Preview - доступні типи аналізу\"\n    )\n    tests.append((\"Analysis Preview\", success))\n    \n    # 5. Hexagon Details - пішохідна конкуренція\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/details/{h3_index}\",\n        params={\"resolution\": 10, \"analysis_type\": \"pedestrian_competition\"},\n        description=\"Hexagon Details - пішохідна конкуренція\"\n    )\n    tests.append((\"Hexagon Details (pedestrian)\", success))\n    \n    # 6. POI in Hexagon\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/poi-in-hexagon/{h3_index}\",\n        params={\"resolution\": 10, \"include_neighbors\": 1},\n        description=\"POI in Hexagon - пошук POI в гексагоні та сусідах\"\n    )\n    tests.append((\"POI in Hexagon\", success))\n    \n    # 7. Competitive Analysis\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/competitive-analysis/{h3_index}\",\n        params={\"resolution\": 10, \"radius_rings\": 2},\n        description=\"Competitive Analysis - аналіз конкуренції\"\n    )\n    tests.append((\"Competitive Analysis\", success))\n    \n    # 8. Тест помилок - неправильний H3\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/details/invalid_h3_index\",\n        params={\"resolution\": 10},\n        expected_status=400,  # Очікуємо помилку\n        description=\"Error Handling - неправильний H3 індекс\"\n    )\n    tests.append((\"Error Handling\", success))\n    \n    # 9. Database H3 Test (якщо БД доступна)\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/database/test-h3/{h3_index}\",\n        description=\"Database H3 Test - перевірка H3 даних в БД\"\n    )\n    tests.append((\"Database H3 Test\", success))\n    \n    # Підсумок\n    print(f\"\\n{'='*80}\")\n    colored_print(f\"🏁 ПІДСУМОК ТЕСТУВАННЯ\", \"cyan\")\n    print(f\"{'='*80}\")\n    \n    passed = sum(1 for _, success in tests if success)\n    total = len(tests)\n    \n    for test_name, success in tests:\n        status = \"✅\" if success else \"❌\"\n        color = \"green\" if success else \"red\"\n        colored_print(f\"{status} {test_name}\", color)\n    \n    print(f\"\\n📊 Результат: {passed}/{total} тестів пройдено\")\n    \n    if passed == total:\n        colored_print(f\"🎉 ВСІ ТЕСТИ ПРОЙШЛИ!\", \"green\")\n        colored_print(f\"✅ API готове до використання\", \"green\")\n        colored_print(f\"💡 Відкрийте Swagger: {BASE_URL}/docs\", \"cyan\")\n    elif passed >= total * 0.7:  # 70%+ success\n        colored_print(f\"🟡 БІЛЬШІСТЬ ТЕСТІВ ПРОЙШЛА\", \"yellow\")\n        colored_print(f\"⚠️ Деякі функції можуть потребувати налаштування БД\", \"yellow\")\n    else:\n        colored_print(f\"🔴 БАГАТО ТЕСТІВ ПРОВАЛИЛОСЯ\", \"red\")\n        colored_print(f\"🔧 Перевірте налаштування сервера та БД\", \"red\")\n    \n    success_rate = (passed / total) * 100\n    colored_print(f\"📈 Відсоток успішності: {success_rate:.1f}%\", \"white\")\n    \n    print(f\"\\n💡 Наступні кроки:\")\n    print(f\"   1. Відкрийте {BASE_URL}/docs для Swagger UI\")\n    print(f\"   2. Протестуйте endpoints через браузер\")\n    print(f\"   3. Інтегруйте з frontend додатком\")\n    \nif __name__ == \"__main__\":\n    main()\n