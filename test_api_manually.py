# test_api_manually.py\n\"\"\"\n🧪 Ручне тестування H3 Modal API endpoints\nПростий скрипт для перевірки роботи API без pytest\n\"\"\"\n\nimport requests\nimport json\nfrom datetime import datetime\nimport time\n\n# Конфігурація\nBASE_URL = \"http://localhost:8000\"\nSAMPLE_H3_INDICES = {\n    7: \"871fb4662ffffff\",\n    8: \"881fb46622fffff\", \n    9: \"891fb466227ffff\",\n    10: \"8a1fb46622d7fff\"\n}\n\ndef test_endpoint(method, url, expected_status=200, params=None, description=\"\"):\n    \"\"\"Универсальная функция для тестирования endpoint\"\"\"\n    print(f\"\\n{'='*60}\")\n    print(f\"🧪 ТЕСТ: {description}\")\n    print(f\"📡 {method.upper()} {url}\")\n    if params:\n        print(f\"📋 Параметры: {params}\")\n    print(f\"{'='*60}\")\n    \n    try:\n        start_time = time.time()\n        \n        if method.lower() == 'get':\n            response = requests.get(url, params=params, timeout=30)\n        else:\n            response = requests.request(method, url, params=params, timeout=30)\n        \n        end_time = time.time()\n        duration = (end_time - start_time) * 1000  # в миллисекундах\n        \n        print(f\"⏱️ Время ответа: {duration:.0f}ms\")\n        print(f\"📊 Статус: {response.status_code}\")\n        \n        if response.status_code == expected_status:\n            print(f\"✅ SUCCESS: Статус {response.status_code} (ожидался {expected_status})\")\n            \n            # Пробуем распарсить JSON\n            try:\n                data = response.json()\n                print(f\"📦 JSON размер: {len(json.dumps(data))} символов\")\n                \n                # Показываем структуру ответа\n                if isinstance(data, dict):\n                    print(f\"🔑 Ключи ответа: {list(data.keys())}\")\n                    \n                    # Специальный вывод для разных типов endpoints\n                    if 'location_info' in data:\n                        loc = data['location_info']\n                        print(f\"📍 Локация: H3-{loc.get('resolution', 'N/A')} {loc.get('h3_index', 'N/A')}\")\n                        print(f\"📐 Координаты: {loc.get('center_lat', 'N/A')}, {loc.get('center_lon', 'N/A')}\")\n                        print(f\"📏 Площадь: {loc.get('area_km2', 'N/A')} км²\")\n                        \n                    if 'neighbor_coverage' in data:\n                        cov = data['neighbor_coverage']\n                        print(f\"🔗 Покрытие: {cov.get('rings', 'N/A')} колец, {cov.get('hexagon_count', 'N/A')} гексагонов\")\n                        print(f\"📊 Общая площадь: {cov.get('area_km2', 'N/A')} км²\")\n                        \n                    if 'poi_details' in data:\n                        poi_count = len(data['poi_details'])\n                        print(f\"🏪 POI найдено: {poi_count}\")\n                        \n                        if poi_count > 0:\n                            # Показываем первые несколько POI\n                            for i, poi in enumerate(data['poi_details'][:3]):\n                                name = poi.get('name', 'Без названия')\n                                brand = poi.get('canonical_name', 'N/A')\n                                influence = poi.get('influence_weight', 'N/A')\n                                print(f\"  └─ POI {i+1}: {name} | Бренд: {brand} | Влияние: {influence}\")\n                    \n                    if 'available_analyses' in data:\n                        analyses = data['available_analyses']\n                        print(f\"📋 Доступно анализов: {len(analyses)}\")\n                        for analysis in analyses:\n                            name = analysis.get('name', 'N/A')\n                            rings = analysis.get('optimal_rings', 'N/A')\n                            area = analysis.get('estimated_area_km2', 'N/A')\n                            print(f\"  └─ {name}: {rings} колец, {area} км²\")\n                    \n                    if 'coverage_breakdown' in data:\n                        breakdown = data['coverage_breakdown']\n                        print(f\"📈 Breakdown шагов: {len(breakdown)}\")\n                        for step in breakdown[:3]:  # Показываем первые 3\n                            rings = step.get('rings', 'N/A')\n                            area = step.get('area_km2', 'N/A')\n                            count = step.get('hexagon_count', 'N/A')\n                            print(f\"  └─ {rings} колец: {area} км², {count} гексагонов\")\n                \n                elif isinstance(data, list):\n                    print(f\"📋 Список элементов: {len(data)}\")\n                    \n                return True, data\n                \n            except json.JSONDecodeError:\n                print(f\"⚠️ Ответ не является валидным JSON\")\n                print(f\"📄 Текст ответа: {response.text[:200]}...\")\n                return response.status_code == expected_status, response.text\n                \n        else:\n            print(f\"❌ FAILED: Статус {response.status_code} (ожидался {expected_status})\")\n            print(f\"📄 Ошибка: {response.text[:300]}...\")\n            return False, response.text\n            \n    except requests.exceptions.ConnectionError:\n        print(f\"❌ CONNECTION ERROR: Сервер недоступен по адресу {BASE_URL}\")\n        print(f\"💡 Убедитесь, что FastAPI сервер запущен: uvicorn src.main:app --reload\")\n        return False, \"Connection failed\"\n        \n    except requests.exceptions.Timeout:\n        print(f\"❌ TIMEOUT: Запрос превысил 30 секунд\")\n        return False, \"Timeout\"\n        \n    except Exception as e:\n        print(f\"❌ UNEXPECTED ERROR: {str(e)}\")\n        return False, str(e)\n\ndef main():\n    \"\"\"Основная функция тестирования\"\"\"\n    print(f\"🚀 НАЧИНАЕМ ТЕСТИРОВАНИЕ H3 MODAL API\")\n    print(f\"🌐 Base URL: {BASE_URL}\")\n    print(f\"📅 Время: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\")\n    \n    # Счетчики\n    total_tests = 0\n    passed_tests = 0\n    \n    # 1. Проверка здоровья сервера\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/health\",\n        description=\"Проверка здоровья сервера\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 2. Проверка базового endpoint\n    h3_index = SAMPLE_H3_INDICES[10]\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/details/{h3_index}\",\n        params={\"resolution\": 10},\n        description=\"Базовый hexagon details endpoint\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 3. Проверка анализа пешеходной конкуренции\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/details/{h3_index}\",\n        params={\"resolution\": 10, \"analysis_type\": \"pedestrian_competition\"},\n        description=\"Анализ пешеходной конкуренции\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 4. Проверка market overview\n    h3_index_8 = SAMPLE_H3_INDICES[8]\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/details/{h3_index_8}\",\n        params={\"resolution\": 8, \"analysis_type\": \"market_overview\"},\n        description=\"Анализ обзора рынка (H3-8)\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 5. Проверка кастомного количества колец\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/details/{h3_index}\",\n        params={\"resolution\": 10, \"analysis_type\": \"custom\", \"custom_rings\": 3},\n        description=\"Кастомное количество колец\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 6. Проверка analysis preview\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/analysis-preview/{h3_index}\",\n        params={\"resolution\": 10},\n        description=\"Preview доступных анализов\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 7. Проверка coverage calculator\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/coverage-calculator\",\n        params={\"resolution\": 10, \"rings\": 2},\n        description=\"Калькулятор покрытия\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 8. Проверка POI в гексагоне\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/poi-in-hexagon/{h3_index}\",\n        params={\"resolution\": 10, \"include_neighbors\": 1},\n        description=\"POI в гексагоне с соседями\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 9. Проверка конкурентного анализа\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/competitive-analysis/{h3_index}\",\n        params={\"resolution\": 10, \"radius_rings\": 2},\n        description=\"Конкурентный анализ\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 10. Проверка обработки ошибок - неправильный H3 индекс\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/details/invalid_h3_index\",\n        params={\"resolution\": 10},\n        expected_status=404,  # Ожидаем ошибку\n        description=\"Обработка неправильного H3 индекса\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # 11. Проверка валидации - неправильный resolution\n    success, _ = test_endpoint(\n        \"GET\", f\"{BASE_URL}/api/v1/hexagon-details/details/{h3_index}\",\n        params={\"resolution\": 15},  # Неправильный resolution\n        expected_status=422,  # Ожидаем ошибку валидации\n        description=\"Валидация неправильного resolution\"\n    )\n    total_tests += 1\n    if success: passed_tests += 1\n    \n    # Итоговые результаты\n    print(f\"\\n{'='*80}\")\n    print(f\"🏁 РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ\")\n    print(f\"{'='*80}\")\n    print(f\"✅ Пройдено: {passed_tests}/{total_tests} тестов\")\n    print(f\"❌ Провалено: {total_tests - passed_tests}/{total_tests} тестов\")\n    \n    if passed_tests == total_tests:\n        print(f\"🎉 ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО!\")\n        print(f\"💡 API готово к использованию с frontend\")\n    else:\n        print(f\"⚠️ НЕКОТОРЫЕ ТЕСТЫ ПРОВАЛИЛИСЬ\")\n        print(f\"🔧 Проверьте настройки базы данных и зависимости\")\n    \n    success_rate = (passed_tests / total_tests) * 100\n    print(f\"📊 Процент успешности: {success_rate:.1f}%\")\n    \nif __name__ == \"__main__\":\n    main()\n